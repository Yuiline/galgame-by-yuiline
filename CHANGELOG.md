# 更新日志

本项目所有重要变更都会记录在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## v0.3 (2026-09-14) — 返回按钮与多场景背景

### 新增
- 背景图切换：剧情推进到第 32 句（思博雅转身离开时）自动从 `素材1.png` 切换到 `素材2.png`
- 返回首页按钮：在游戏页右下角显示 `back.png`，点击跳回 `index.html`
- 事件冒泡保护：点击返回按钮时不会触发全局点击事件，避免剧情意外推进
- 新增占位文件 `game2.html`，供第二段剧情使用
- 新增图片资源 `back.png`（返回按钮）与 `素材2.png`（夜街背景）

### 修复
- 修复 `<script>` 标签误写在 CSS `<style>` 内导致编辑器高亮错误的问题
- 清理跳转 `game2.html` 的 URL 中多余的 `%20` 与 `?`，避免链接变丑
- 修复返回按钮 CSS 选择器与 HTML id 不一致的潜在问题

### 已知问题
- 返回按钮 `back.png` 的垂直位置 `top: 950px` 偏低，建议后续调整到更合理位置
- index.html 末尾的 `/关于理想.../` 文本是作者有意保留的彩蛋，待确认是否移动到注释
- 没有人物立绘层、没有背景音乐与音效
- `game2.html` 仅为占位文件，没有第二段剧情

### 下一版本计划（v0.4）
- 调整返回按钮位置至分隔线最左侧
- 加入角色立绘层（思博雅、林韦）
- 接入背景音乐与点击音效
- 编写 `game2.html` 的第二段剧情

### 致谢
- 感谢 **Micats** 的支持与鼓励！

---

## v0.2 (2026-09-14) — 完整剧情与对话系统

### 新增
- 完整剧情文案（共 75 句），覆盖开场炒饭摊、街头偶遇、夜里桥上、河水落幕四个场景
- 角色台词系统：使用 `<b>` 标签对角色名加粗（林韦 / 思博雅 / 父亲 / 内心独白）
- 键盘推进支持：在游戏页按 `空格` 或 `回车` 即可推进对话
- 半透明深色对话框 + 文字阴影 + 背景模糊，提升白字在不同背景下的可读性
- 备用底色 `#222`：背景图加载失败时不会变成纯白屏

### 修复
- 修复 `<script>` 标签误写在 `<style>` 内导致编辑器高亮错误的 bug
- 修复 `getElementById('.story-text')` 多带点号的参数错误（应只传 id 名）
- 修复数组缺少逗号、中文引号未转义导致的显示/语法错误
- 修复绝对定位布局下文本可能被挤出视口的问题
- 修复 `game.html` 中空 `<script>` 标签导致代码高亮混乱的问题

### 已知问题
- 剧情以单段文字形式呈现，未做旁白/对话样式区分
- 没有背景图切换，多场景共用同一张素材1.png
- 没有人物立绘、背景音乐、音效

### 下一版本计划（v0.3）
- 给旁白、对话、内心独白分别设计不同的文字样式
- 实现多场景背景图切换
- 添加角色立绘层
- 增加"退出游戏"按钮与确认弹窗

### 致谢
- 感谢 **Micats** 的支持与鼓励！

---

## v0.1 (2026-09-13) — 初始版本

### 新增
- 标题画面（index.html）：显示游戏名称 + "开始游戏"按钮
- 游戏页占位（game.html）：显示 "comming soon"，作为跳转目标
- 页面跳转逻辑（game.js）：点击 "开始游戏" 后跳转到 game.html
- flex 居中布局：标题与按钮在水平方向居中
- 独立 JS 文件：将脚本从 HTML 抽离到 game.js
- `.gitignore`：忽略系统/编辑器临时文件

### 已知问题
- game.html 仅为占位页面，没有实际游戏内容
- 退出游戏功能尚未实现（计划在下一版本完成）
- 标题/按钮样式为最简版，无背景图与动效

### 下一版本计划（v0.2）
- 新增"退出游戏"按钮与确认弹窗
- 美化标题画面（背景色/图、按钮样式）